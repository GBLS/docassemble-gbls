---
comment: |
  This interview is meant to be interacted with via API.

  API call should set the variable `ls_variables` to a dictionary
  of fields from Legal Server. Must also set the variables `redis_secret`
  and `redis_key`
---
modules: 
  - .legal_server
---
mandatory: True
code: |
  # from docassemble.webapp.backend import encrypt_object
  # encrypted_vars = encrypt_object(ls_variables, redis_secret)
  # r.set_data(redis_key,encrypted_vars,expire=30)

  save_ls_fields(ls_variables, redis_secret, redis_key, expire=3600)
  command('exit') # cleanup our session after we save to Redis